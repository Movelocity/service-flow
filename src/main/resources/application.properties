# Workflow Configuration
workflow.definitions.path=saved_workflows

# Server Configuration
server.port=8080

# Logging Configuration
logging.level.cn.yafex.workflow=DEBUG
logging.file.name=logs/workflow-service.log 